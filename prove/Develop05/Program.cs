public class Program
{
    public static void Main(string[] args)
    {
        // Exceeding Requirements:
        // I added a level system based on the user's total score. The current level is
        // displayed with the score, and the program shows a special level-up message
        // whenever the user earns enough points to reach a new level.

        Goals goals = new Goals();
        goals.Start();
    }
}
